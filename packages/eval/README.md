# eval

## 定位

`eval` 用于承载评测数据集、评测执行与指标。当前尚未实现。

## 当前目录

- `src/`：源码目录，仅允许放置 `.ts` 源码。
- `src/index.ts`：源码入口文件。
- `dist/`：构建产物输出目录，仅在执行构建后生成。

## 当前状态

当前仅完成工程初始化，尚未添加任何评测实现。

## 构建约定

- 对外入口固定指向 `dist/index.js` 与 `dist/index.d.ts`。
- 构建产物必须输出到 `dist/`。
- 不允许把 `.js`、`.d.ts` 等构建产物回写到 `src/`。
