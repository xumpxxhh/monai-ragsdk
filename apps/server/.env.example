# 复制为 .env 后填入真实密钥；不要把密钥提交进仓库。

PORT=3000
CORS_ORIGIN=http://localhost:5173

# PostgreSQL + pgvector（缺省与 apps/example 相同）
PGVECTOR_CONNECTION_STRING=postgresql://monai:monai@localhost:5432/monai_ragsdk

# Embedding（OpenAI 兼容）
EMBEDDING_API_KEY=
EMBEDDING_BASE_URL=https://llm-5vs4jf61x3o1aul1.cn-beijing.maas.aliyuncs.com/compatible-mode/v1
EMBEDDING_MODEL=text-embedding-v3
EMBEDDING_DIMENSION=1024

# Chat / 策略 LLM（OpenAI 兼容，dots3-note-prev）
DOTSAI_API_KEY=
DOTSAI_BASE_URL=https://note3-prev-api.askdiandian.com/v1
DOTSAI_CHAT_MODEL=dots3-note-prev
